/**
 * Author: Jane Valencia
 * Version: 1.0.0
 * 
 * This is just to be used as a test run,
 * to verify that Cypress is connected and working.
 */
describe('empty spec', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
})